<%= form_for @mesocycle do |f| %>
 
  <% if @mesocycle.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@mesocycle.errors.count, "error") %> prohibited
        this mesocycle from being saved:
      </h2>
      <ul>
        <% @mesocycle.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 <fieldset>
  <legend>Variations</legend>

  <h3>Max Effort Squat Exercises</h3>
  <p>
      <%= f.collection_check_boxes :exercise_ids, @maxLowerSquatExercises, :id, :name do |b| %>
        <div class="collection-check-box">
          <%= b.check_box %>
          <%= b.label %>
        </div>
      <% end %>
  </p>
 </fieldset>
  <p>
    <%= f.submit %>
  </p>
 
<% end %>